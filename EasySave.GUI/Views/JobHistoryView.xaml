<UserControl x:Class="EasySave.GUI.Views.JobHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:EasySave.GUI.Converters">
 
    <UserControl.Resources>
<converters:StatusColorConverter x:Key="StatusColorConverter" />
</UserControl.Resources>
 
    <Grid Margin="24">
<Grid.RowDefinitions>
<RowDefinition Height="Auto" />
<RowDefinition Height="Auto" />
<RowDefinition Height="*" />
<RowDefinition Height="Auto" />
</Grid.RowDefinitions>
 
        <!-- TITLE -->
<StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="0,0,0,20">
<TextBlock Text="☷"
                       FontSize="26"
                       Foreground="{StaticResource AccentLightBrush}"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0" />
 
            <StackPanel>
<TextBlock Text="Historique des sauvegardes"
                           Style="{StaticResource PageTitleStyle}" />
<TextBlock Text="Consultez les dernières exécutions et relancez un travail si nécessaire."
                           Style="{StaticResource SecondaryTextStyle}" />
</StackPanel>
</StackPanel>
 
        <!-- ACTION BAR -->
<Border Grid.Row="1"
                Style="{StaticResource CardBorderStyle}"
                Margin="0,0,0,18">
<WrapPanel>
<Button Content="{Binding SortDirectionLabel}"
                        Style="{StaticResource PrimaryActionButtonStyle}"
                        Margin="0,0,10,10"
                        Command="{Binding SortByDateCommand}" />
 
                <Button Content="Actualiser"
                        Style="{StaticResource SecondaryActionButtonStyle}"
                        Margin="0,0,10,10"
                        Command="{Binding RefreshCommand}" />
</WrapPanel>
</Border>
 
        <!-- HISTORY TABLE -->
<Border Grid.Row="2"
                Style="{StaticResource CardBorderStyle}">
<DataGrid x:Name="HistoryDataGrid"
                      ItemsSource="{Binding History}"
                      SelectedItem="{Binding SelectedHistoryItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserSortColumns="True">
<DataGrid.Columns>
 
                    <DataGridTextColumn Header="#"
                                        Binding="{Binding Number, StringFormat={}{0:00}}"
                                        Width="70" />
 
                    <DataGridTextColumn Header="Nom"
                                        Binding="{Binding Name}"
                                        Width="2*" />
 
                    <DataGridTextColumn Header="Type"
                                        Binding="{Binding Type}"
                                        Width="150" />
 
                    <DataGridTextColumn Header="Date d'exécution"
                                        Binding="{Binding LastExecutionTime, StringFormat={}{0:dd/MM/yyyy HH:mm}}"
                                        Width="210" />
 
                    <DataGridTemplateColumn Header="Statut final"
                                            Width="150">
<DataGridTemplateColumn.CellTemplate>
<DataTemplate>
<TextBlock Text="{Binding Status}"
                                           Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
</DataTemplate>
</DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>
 
                    <DataGridTemplateColumn Header="Action"
                                            Width="160">
<DataGridTemplateColumn.CellTemplate>
<DataTemplate>
<Button Content="Relancer"
                                        Height="32"
                                        Width="110"
                                        Command="{Binding DataContext.ReExecuteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
</DataTemplate>
</DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>
 
                </DataGrid.Columns>
</DataGrid>
</Border>
 
        <!-- STATUS -->
<Border Grid.Row="3"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Margin="0,18,0,0"
                Padding="0,14,0,0">
<StackPanel Orientation="Horizontal">
<Border Width="28"
                        Height="28"
                        CornerRadius="14"
                        Background="{StaticResource AccentBrush}"
                        Margin="0,0,10,0">
<TextBlock Text="i"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
</Border>
 
                <TextBlock Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimaryBrush}" />
</StackPanel>
</Border>
</Grid>
</UserControl>